#include "main.h"

/**
* _isupper - This compares characters to see if uppercase or not
* @c: Represents integer variable
* Return: Always 1 when the if condition evaluates to True and 0 when False
*/

int _isupper(int c)
{
	if (c >= && c <= 90)
	{
		return (1);
	}
	return (0);
}
