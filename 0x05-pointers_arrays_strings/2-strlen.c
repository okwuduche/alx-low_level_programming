#include <stdio.h>
#include <stdio.h>
#include "main.h"

/**
* _strlen - Used to return lenght of a sring
* @s: Represents string
* Return: Lenght of string
*/

int _strlen(char *s)

{
	size_t lenght = 0;

	while (*++)
	{
		lenght++;
	}
	return (lenght);
}
