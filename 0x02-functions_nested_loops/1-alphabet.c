#include "main.h"

/**
* print_abc - Printing of alphabets in lowercases
* Return: It has to be 0 as always when succesful
*/

void print_aphabet(void)

{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar (alpah);
	}
	_putchar('\n');
}
